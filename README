For semihosting debug:
$ sudo openocd
$ cargo build --bin test
$ cargo run --bin test

For itm debug:
$ mkfifo /tmp/itm.fifo
$ itmdump -f /tmp/itm.fifo -F
$ cargo build --bin test
$ cargo run --bin test
